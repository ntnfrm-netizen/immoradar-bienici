{
  "name": "immoradar-bienici",
  "version": "2.0.0",
  "description": "ImmoRadar — synchronisation auto Bien'ici via Gmail + Gemini sur Netlify",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod",
    "get-token": "node scripts/get-refresh-token.mjs"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "@netlify/blobs": "^8.1.0",
    "@netlify/functions": "^2.8.2",
    "googleapis": "^144.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.19.19",
    "typescript": "5.6"
  },
  "engines": {
    "node": ">=20"
  }
}
